# Requirements optimized for Render deployment
# Uses pre-built wheels to avoid Rust compilation issues

# FastAPI & ASGI
fastapi==0.109.0
uvicorn[standard]==0.27.0
python-multipart==0.0.6

# Database
sqlalchemy==2.0.25
psycopg2-binary==2.9.9
alembic==1.13.1

# Authentication (using pre-built wheels)
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
python-dotenv==1.0.0
cryptography>=41.0.0

# Validation
pydantic==2.5.3
pydantic-settings==2.1.0
email-validator==2.1.0

# Storage (optional - can skip if not using S3)
boto3==1.34.47
Pillow>=10.0.0

# HTTP Client
httpx==0.26.0

