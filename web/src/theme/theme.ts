// Theme configuration - Currently not in use (using Tailwind CSS instead)
// Keeping file for potential future use with Material-UI

// Simple placeholder export to avoid build errors
export const theme = {} as any;
